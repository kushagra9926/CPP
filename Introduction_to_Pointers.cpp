#include <iostream>
using namespace std;

// Hexadecimal: These number become address of memory location. Hexadecimal numbers have base 16 [0 to 9, a(10), b(11), c(12), d(13), e(14), f(15)].

// Address of Operator: '&' gives address of a variable.
    /* 
     For ex: 
        int a = 10;
        cout << &a;   // output: 0x61ff0c
    */
int main(){
    
    return 0;
}
